# src/mcp-server

MCP-server (.NET 10) waarmee een AI-client (Claude) met de eigen, lokaal ontsleutelde data kan werken — onder andere de bonnetjes-feature en query tools.

Nog niet gebouwd. Zie `CLAUDE.md` voor de rol binnen de architectuur.
